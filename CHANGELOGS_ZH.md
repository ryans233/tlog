# 更新日志

本文件记录了 tlog 的所有重要变更。

## [1.1.0] - 2026-08-01

### 新增
- 可自定义日志颜色。按 `o` 打开设置界面；每个可着色项（6 个日志等级、Tag、时间戳）
  可用十六进制值编辑（`1`–`8`），并可用 `[` / `]` 循环预设
  （默认 / Solarized Dark / Monokai / 高对比度）。
- 设置持久化：颜色、当前预设和显示选项在每次更改后保存到
  `<config-dir>/tlog/config.conf`，启动时自动加载。
- 过滤器标题前新增运行状态指示：`RUNNING`（绿色）、`PAUSED`（黄色，按 `p` / 空格切换）、
  `STOPPED`（红色，logcat 输出结束时）。
- 日志消息显示不再截断为 512 字符。

### 变更
- 显示设置与颜色设置合并为一个带分类页签的设置界面
  （`Tab` / `Shift+Tab` 在「显示」与「颜色」分类之间切换）。`o` 为唯一入口键；`c` 不再打开设置。
- 移除主窗口 `1`–`6` 显示快捷键——这些选项现在在「设置 → 显示」中调整。
- 显示设置变更现在会立即重绘已有滚动区内容（此前只有新条目才会生效）。

### 修复
- 手动编辑过的调色板重启后显示为「自定义」，不再回退为「默认」。
- 退出时清屏并清除滚动区，Windows Terminal 不再残留 tlog 内容。

## [1.0.0] - 2026-07-27

### 新增
- 初始版本：基于 ratatui + crossterm 的 Android logcat TUI 查看器（`-v threadtime` 解析）——
  内联滚动回放视口、带实时状态的过滤器栏、暂停/清空/过滤器旁路控制、帮助与显示配置覆盖层，
  以及通过 `adb shell ps` 解析 PID→包名。
- 多平台 CI 构建工作流。
